import { Keyring } from "@polkadot/keyring";
import { KeyringPair } from "@polkadot/keyring/types";
import { ChainProperty, KeypairType } from "../types";
interface IChainAccount {
    pair: KeyringPair;
    keyring: Keyring;
}
export declare class ChainAccount implements IChainAccount {
    private _keyring;
    private _mnemonic;
    private _keyringType;
    static generate(): string;
    constructor(mnemonic: string, type?: KeypairType);
    get pair(): KeyringPair;
    get keyring(): Keyring;
    formatAccount(chainProperty: ChainProperty): void;
}
export {};
