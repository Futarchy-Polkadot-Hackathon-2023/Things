import { Ora } from "ora";
export declare class Spinner {
    ora: Ora;
    verbose: boolean;
    constructor(verbose?: boolean);
    start(text: string): Promise<void>;
    succeed(text: string): void;
    fail(text: string): void;
    output(text: string): void;
    text(text: string): void;
    runCommand(command: () => Promise<unknown>, runningMessage: string, successMessage?: string, failMessage?: string, shouldExitOnError?: boolean): Promise<unknown>;
    ensureSpinner(): void;
}
