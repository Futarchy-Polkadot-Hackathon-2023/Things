/// <reference types="node" />
import { Abi } from "@polkadot/api-contract";
import { KeyringPair } from "@polkadot/keyring/types";
import { ChainApi } from "./substrate-api";
export type AbiType = Abi;
export declare class DeployApi extends ChainApi {
    constructor(endpoint: string);
    getGasCost(): Promise<void>;
    deploy(abi: Abi, wasm: Buffer, signerPair: KeyringPair, gasLimit: number, args: string[]): Promise<unknown>;
}
